[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Neo4j health endpoint (HTTP API root returns 200 when ready)
healthcheckPath = "/"
healthcheckTimeout = 300
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
